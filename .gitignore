# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env

# Local Supabase CLI link state (this repo has no functions/migrations of its own)
supabase/.temp

# Local Vercel CLI link state — created by `vercel link`, holds project/org ids
.vercel

# env — never commit real secrets
.env*.local
.env.production

# signing / credentials
*.jks
*.p8
*.p12
*.keystore
.env*

# Would let `tsc -b` skip work and pass vacuously if ever committed.
*.tsbuildinfo
